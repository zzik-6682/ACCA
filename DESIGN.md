# ACCA 考证统计系统 - 设计思考

## 产品定位

一款面向ACCA专业学生的考证进度管理工具，帮助学生记录考证成绩、班级管理员查看统计数据、考季提醒。

## 气质与意象

**关键词**：专业、清晰、高效、学院风

**具象场景**：
阳光穿过图书馆的玻璃窗，照亮整洁的木质长桌。桌上整齐摆放着考证教材和成绩单，学生用笔在进度表上记录通过的科目。远处墙上挂着班级整体的通过率柱状图，蓝色和灰色的色块清晰展示每个人的进度。这就是我们的视觉基调——学院派的简洁与专业感。

## 配色方案

| 颜色 | 意象来源 | Tailwind 类名 | 用途 |
|-----|---------|--------------|-----|
| 主色蓝 | 图书馆书架的深蓝 | `#1E40AF` (blue-800) | 标题、按钮、强调 |
| 辅助蓝 | 阳光下的浅蓝窗帘 | `#3B82F6` (blue-500) | 链接、图标选中态 |
| 中性灰 | 木桌的自然灰 | `#6B7280` (gray-500) | 说明文字 |
| 浅灰背景 | 淡淡的桌面色 | `#F9FAFB` (gray-50) | 页面背景、卡片容器 |
| 成功绿 | 成绩单上的绿色印章 | `#10B981` (emerald-500) | 通过状态标签 |
| 警示橙 | 提醒便签的橙色 | `#F59E0B` (amber-500) | 待考、提醒 |

## 字体排版

- **标题**：思源黑体/系统黑体，加粗，营造专业感
- **正文**：系统默认字体，易读优先
- **数据表格**：数字用等宽字体对齐，中文用默认字体

**排版节奏**：
- 页面标题：text-xl font-bold，mb-6
- 卡片标题：text-lg font-semibold，mb-4
- 说明文字：text-sm text-gray-500
- 表格数据：text-base，紧凑排列

## 组件使用原则

**通用 UI 组件优先使用 `@/components/ui/*`**：
- **Button**：主要操作按钮（提交、保存）
- **Input**：单行输入（学号、姓名）
- **Textarea**：备注说明
- **Card**：信息卡片、统计卡片
- **Badge**：状态标签（通过/待考）
- **Tabs**：年级切换、统计维度切换
- **Progress**：整体进度条展示
- **Table**：学生考证列表
- **Dialog**：确认删除、详情弹窗
- **Toast**：操作成功/失败提示
- **Skeleton**：数据加载态

**页面结构容器**：
- `View` 作为页面主体容器
- `Text` 用于文字展示（垂直排列加 `block`）
- `Image` 用于成绩截图展示

## 间距系统

- **页面边距**：`p-4`（16px）
- **卡片内边距**：`p-4` 或 `p-6`
- **组件间距**：`gap-4`（列表项）、`gap-2`（标签组）
- **底部安全区**：底部固定元素 `bottom-50` 避开 TabBar

## 导航结构

**TabBar 配置**：
1. 首页 - 功能入口
2. 上传 - 记录考证成绩
3. 统计 - 数据可视化
4. 我的 - 个人记录查看

## 小程序约束

- 图片上传使用对象存储，不打包到项目中
- TabBar 图标放 `src/assets/tabbar/`（本地 PNG）
- 避免长列表全量渲染，使用分页或虚拟滚动
- 图表使用简洁的 CSS/Canvas 实现，不引入大型图表库

## 设计禁忌

- ❌ 禁止使用渐变背景（过于花哨）
- ❌ 禁止大量装饰性图标（干扰数据展示）
- ❌ 禁止用 `View/Text` 手搓 Button/Input/Card 等通用组件
- ❌ 禁止硬编码 px 值，使用 Tailwind 预设
- ❌ 禁止使用科技蓝/紫渐变（不符合学院气质）